#!/usr/bin/env bash
# -*- coding: utf-8 -*-

# run a command, then cd into the last line and ls


output="$(${1} ${@:2})"
echo "$(echo -e $output | cut -d "\`" -f1)"
d="$(echo -e $output | tr "\n" " " | rev | cut -d "\`" -f1 | rev | xargs)"
echo "${d}"
echo "--------------------"
\cd "${d}"
l
