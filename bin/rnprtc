#!/usr/bin/env bash
# -*- coding: utf-8 -*-

# run a command, then cd into the last line and ls


output="$(${1} ${@:2})"
r="$(echo -e ${output} | cut -d "\`" -f1)"
[[ ! -z "${r// }" ]] && echo "${r}"
d="$(echo -e ${output} | tr "\n" " " | rev | cut -d "\`" -f1 | rev)"
echo "${d// }"
echo "--------------------"
\cd "${d// }"
l
